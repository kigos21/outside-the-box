// Services Page
export type ServicePill = {
  title: string;
  price: string;
};

export type RegularRateList = {
  title: string;
  items: string[];
  price?: string;
};

// Reserve seat
export type SeatReservationInputs = {};
